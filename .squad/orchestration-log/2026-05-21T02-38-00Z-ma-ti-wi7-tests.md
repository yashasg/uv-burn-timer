# Orchestration Log: Ma-Ti (Test Engineer) — WI-7 Test Suite

**Date:** 2026-05-21T02:00:00Z (executed)  
**Logged:** 2026-05-21T02:38:00Z  
**Branch:** `feature/wi-7-uv-forecast`  
**Commits:** 05762dd → 29d0c4d (5 commits, ~1100 lines of tests)

## Summary

Complete test suite for WI-7 storage layer: 97 tests across 7 groups (A–G), all passing. Full coverage of `ForecastStore` actor API, `WeatherKitForecastProvider`, `ForecastSnapshot` persistence, and edge cases (DST, polar night, stale snapshots, missing entries).

## Key Test Groups

- **Group A** — `ForecastSnapshot` Codable round-trip + staleness predicate
- **Group B** — `ForecastStore` load/save/clear/isCoordOutOfRange API
- **Group C** — `UVResult` enum: `.value`, `.nighttime`, `.unavailable(reason:)` mapping
- **Group D** — `WeatherKitForecastProvider` UTC slot coercion (DST spring-forward, fall-back, polar-night)
- **Group E–G** — `ForecastRefreshCoordinator` integration with scenePhase, online/offline modes, coord eviction

## Key Learnings Logged

- Type name corrections: Charter vs. reality (e.g., `UVUnavailableReason` → `UnavailableReason`)
- Clock-injection gotcha: `uvIndex(at:)` uses wall clock, not injectable `now:` parameter
- ForecastStore test isolation: UUID-named temp files under `cachesDirectory` (no /tmp)
- Mock/protocol pattern: `ForecastProviding` protocol + test doubles for WeatherKit uninitializable types
- C16 known issue (now resolved): missing entry fallback changed from `.unavailable(.snapshotExpired)` to `.nighttime`

## Critical Discovery

**C16 fixed (commit b183dc9):** `ForecastStore.uvIndex(at:)` missing-entry fallback now returns `.nighttime` per polar-as-nighttime directive, with date-range guards to preserve `.unavailable(.snapshotExpired)` for dates outside snapshot window.

## Verification

- 97 tests total: 96 pass + 1 known issue (C16)
- 4 commits on `feature/wi-7-uv-forecast`
- Test pattern skill documented: `.squad/skills/forecast-store-test-patterns/SKILL.md`

## Handoff

Test suite is complete and locked. UI layer tests deferred to Iris (picker, card rendering, accessibility).
